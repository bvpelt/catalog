



# Datasource definities
## H2
spring.datasource.platform=h2
#spring.datasource.driver-className=org.postgresql.Driver
#spring.datasource.configuration.maximum-pool-size=5
#.datasource.configuration.auto-commit=false
#spring.datasource.configuration.pool-name=postgres-pool
# prevent clob error messages see https://auth0.com/blog/integrating-spring-data-jpa-postgresql-liquibase/
#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false



spring.datasource.url=jdbc:h2:mem:catalogus
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.h2.console.enabled=true
# Show all queries
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type=trace
spring.liquibase.change-log=classpath:/db/changelog/changelog-postgres-master.yaml
spring.liquibase.enabled=true



#spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.yaml # Change log configuration path.
#spring.liquibase.check-change-log-location=true # Whether to check that the change log location exists.
spring.liquibase.contexts= # Comma-separated list of runtime contexts to use.
#spring.liquibase.database-change-log-lock-table=DATABASECHANGELOGLOCK # Name of table to use for tracking concurrent Liquibase usage.
#spring.liquibase.database-change-log-table=DATABASECHANGELOG # Name of table to use for tracking change history.
#spring.liquibase.default-schema= # Default database schema.
#spring.liquibase.drop-first=false # Whether to first drop the database schema.
#spring.liquibase.enabled=true # Whether to enable Liquibase support.
#spring.liquibase.labels= # Comma-separated list of runtime labels to use.
#spring.liquibase.liquibase-schema= # Schema to use for Liquibase objects.
#spring.liquibase.liquibase-tablespace= # Tablespace to use for Liquibase objects.
#spring.liquibase.parameters.*= # Change log parameters.

spring.liquibase.rollback-file= # File to which rollback SQL is written when an update is performed.
#spring.liquibase.test-rollback-on-update=false # Whether rollback should be tested before update is performed.
spring.liquibase.url= jdbc:h2:mem:catalogus # JDBC URL of the database to migrate. If not set, the primary configured data source is used.
spring.liquibase.user= sa # Login user of the database to migrate.
spring.liquibase.password=  # Login password of the database to migrate.
